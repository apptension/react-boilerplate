import { expect } from 'chai';
import { fromJS } from 'immutable';

import {
  reducer as {{ camelCase name }}Reducer,
  {{ pascalCase name }}Actions,
  {{ pascalCase name }}Types,
} from '../{{ camelCase name }}.redux';


describe('{{ pascalCase name }}: redux', () => {
  const state = fromJS({
  });

  describe('reducer', () => {
    it('should return initial state', () => {
      expect({{ camelCase name }}Reducer(undefined, {}).toJS()).to.deep.equal(state.toJS());
    });

    it('should return state on unknown action', () => {
      expect({{ camelCase name }}Reducer(state, { type: 'unknown-action' }).toJS()).to.deep.equal(state.toJS());
    });
  });
});
